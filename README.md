# Casual & Naive Blockifier Bench

![Alt Text](./viz/blockifier_benches_heatmap.png)

## How to run
```bash
cargo build
# run the benchmarks - takes time
cargo bench
# collect the data
cargo run
# plot data
python ./blockifier.py
```